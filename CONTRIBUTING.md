# Contributing

Use Node 22 and npm 10.9 or newer. Run `npm install`, `npm --prefix worker install`, and `npm run check` before opening a pull request. Never commit `.env`, `.dev.vars`, Wrangler state, manifests, media, or credentials.

Changes to object keys, manifest fields, routes, or generated HTML are public compatibility changes and require tests and a changelog entry.
